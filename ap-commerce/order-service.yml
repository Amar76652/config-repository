server:
  port: 8084

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.apcommerce.order.client: DEBUG

feign:
  client:
    config:
      default:
        loggerLevel: full
